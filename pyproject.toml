[project]
name = "diligence-room"
version = "0.1.0"
description = "Zero-trust runtime for autonomous institutional agent fleets, demonstrated through M&A due diligence."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "Diligence Room" }]
dependencies = [
    "fastapi>=0.115",
    "google-adk>=1.5.0",
    "google-cloud-aiplatform[adk,agent_engines]>=1.100.0",
    "google-cloud-documentai>=2.29",
    "google-cloud-firestore>=2.16",
    "google-cloud-pubsub>=2.21",
    "google-genai>=1.5.0",
    "openpyxl>=3.1",
    "opentelemetry-api>=1.25",
    "opentelemetry-exporter-gcp-trace>=1.6",
    "opentelemetry-sdk>=1.25",
    "pillow>=10.3",
    "pypdf>=4.0",
    "python-docx>=1.1",
    "uvicorn>=0.30",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = [
    "agents",
    "armor",
    "compliance",
    "coordination",
    "dashboard",
    "evals",
    "gateway",
    "identity",
    "infra",
    "ingestion",
    "memory",
    "observability",
    "redteam",
    "registry",
    "runtime",
]

[dependency-groups]
dev = [
    "fpdf2>=2.7",
    "httpx>=0.27",
    "mypy>=1.11",
    "pre-commit>=3.7",
    "pytest>=8.0",
    "ruff>=0.5",
    "types-openpyxl>=3.1",
]

[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = [".git", ".venv", ".codegraph", ".omo"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "W", "C4"]

[tool.ruff.lint.isort]
known-first-party = [
    "agents",
    "armor",
    "compliance",
    "coordination",
    "dashboard",
    "evals",
    "gateway",
    "identity",
    "infra",
    "ingestion",
    "memory",
    "observability",
    "redteam",
    "registry",
    "runtime",
]

[tool.mypy]
python_version = "3.11"
strict = true
warn_unreachable = true
pretty = true

[[tool.mypy.overrides]]
module = [
    "google.*",
    "vertexai.*",
    "google_cloud_aiplatform.*",
    "docx.*",
    "PIL.*",
    "opentelemetry.exporter.cloud_trace.*",
]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
