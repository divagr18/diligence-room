# D2 Teardown Evidence — live resources removed after gate verification (2026-08-16)

Executed AFTER docs/evidence/d2-live-gate.txt captured the full gate chain.

## Removals (in order)

| Resource | Identifier | Command | Result |
|---|---|---|---|
| Agent Engine | projects/910285417505/locations/us-central1/reasoningEngines/5096132490892935168 | `agent_engine.py delete` (force=True for child sessions) | "AgentEngine resource deleted"; state file removed |
| Cloud Run service | gateway (us-central1) | `gcloud run services delete gateway --region us-central1 --quiet` | "Deleted service [gateway]"; services list empty |
| Billing budget | billingAccounts/012D4F-261885-E006CE/budgets/02663680-ca6f-47f0-a75e-0f10ef83719d | `gcloud billing budgets delete ... --quiet` | "Deleted budget"; remaining list shows only the pre-existing user budget |
| Project | diligence-room (910285417505) | `gcloud projects delete diligence-room --quiet` | "Deleted"; lifecycleState=DELETE_REQUESTED (verified read-only) |

Buckets, topic, subscription, notifications, and Firestore data were removed
with the project deletion. The bucket/topic/subscription are re-creatable via
`infra/data_room.py --confirm-live`; the Agent Engine via
`infra/deploy/agent_engine.py deploy` (state file regenerated).

## Teardown notes discovered live

- Agent Engine delete requires `force=True` once sessions exist (FailedPrecondition otherwise) — fixed in cmd_delete.
- `gcloud billing budgets delete` needs `--quiet` to avoid an interactive prompt.
- Firestore database is NOT recreated automatically after project undelete —
  `gcloud firestore databases create --location=nam5` must run before any live
  Firestore use (added to the live runbook).
