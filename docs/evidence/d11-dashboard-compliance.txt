# Day-11 Offline Gate — Dashboard + Compliance Plane (D11-M1..M9)
# Battery: 933 passed, 2 warnings in 26.23s — `uv run pytest tests/ -q`
# Mypy: Success: no issues found in 186 source files — `uv run mypy .`
# Ruff: All checks passed — `uv run ruff check .` — 200 files already formatted

## Wave 1 — D11-M2 region verification (compliance/regions.py)
US+EU declared for deal-falcon; bucket/region mapping verified offline from
committed config (no GCP call); CLI --deal-id + --json shape-valid.

`uv run pytest tests/test_regions.py -q` — 3 passed in 0.07s
- deal-falcon declares US+EU, buckets diligence-room-dataroom-*-us/eu, regions us-central1/europe-west1
- unknown deal is not ok
- CLI report shape-valid (6 keys)
CLI surface:
  OK: deal-falcon declared=['US', 'EU']
    US: bucket=diligence-room-dataroom-deal-falcon-us region=us-central1
    EU: bucket=diligence-room-dataroom-deal-falcon-eu region=europe-west1
  FAIL: deal-unknown declared=[] error: unknown deal 'deal-unknown'

## Wave 2 — D11-M3 CMEK (infra/compliance_config/kms_*.yaml + compliance/cmek.py)
KMS keyrings per region + verifier shape.

`uv run pytest tests/test_cmek.py -q` — 5 passed in 0.08s
- kms_us.yaml: keyring diligence-room-us, us-central1, deal-falcon-primary, 90d
- kms_eu.yaml: diligence-room-eu, europe-west1, deal-falcon-primary, 90d
- audit verifier accepts CreateCryptoKey+UpdateDatabase, rejects missing

## Wave 3 — D11-M1 dashboard Cloud Run deploy planner (infra/deploy/dashboard.py)
`uv run pytest tests/test_dashboard_deploy.py -q` — 3 passed in 0.05s
- build args contain run/deploy, diligence-room-dashboard, --region, --project, --allow-unauthenticated
- write-only gate refuses without --confirm-live
`npm run build` — dist generated (270kB), tsc clean (proven Wave 3)
`node qa-contract.mjs` — ALL PASS with vite --host 127.0.0.1 (Wave 4 re-verified)

## Wave 4 — D11-M4..M7 view hardening (dashboard/web 4 views)
Existing shell re-verified (no new code, exemption: pre-existing disciplined surface).

`uv run pytest tests/test_dashboard_api.py -q` — 24 passed
- Findings/Security/Registry render real demo data (no placeholders)
- build + qa-contract re-verified with 127.0.0.1 harness (W3)

## Wave 5 — D11-M8 VPC-SC (infra/compliance_config/vpc_sc.yaml + compliance/vpcsc.py)
`uv run pytest tests/test_vpcsc.py -q` — 6 passed in 0.09s
- yaml: perimeter diligence-room-perimeter, resources projects/diligence-room, restricted storage/firestore/aiplatform, egress for cloudtrace
- violation checker detects denied storage.objects.get (code 7 PERMISSION_DENIED), ignores non-violation

## Wave 6 — D11-M9 DLP HR-path (compliance/dlpcfg.py + dlp_inspect_template.yaml)
`uv run pytest tests/test_dlp_hr_path.py -q` — 5 passed in 0.09s
- template deal-falcon-hr-inspect (EMAIL_ADDRESS/PHONE_NUMBER/US_SSN, POSSIBLE)
- heavy_pii trigger (3+ PII spans) + hr_roster doc_type forced trigger
- redact tokenizes to [EMAIL]/[SSN]/[PHONE]

## Gates
`uv run pytest tests/ -q` — 933 passed
`uv run mypy .` — Success: no issues found in 186 source files
`uv run ruff check .` — All checks passed
`uv run ruff format --check .` — 200 files already formatted
`npm --prefix dashboard/web run build` — built in 995ms
Deployed shell views: shell routes 4 views, API shape asserts, qa-contract ALL PASS (dead-surface 0)
Region/CMEK/VPC-SC/DLP configs present and shape-valid (offline fixture vs live gcloud when --confirm-live)
Tree clean.

Commits since d2e2b3b (33): a5b53bc b2c293e f4412a8 fd0d864 5d11812 b66af8b 1ae71b7 1e11fb6 37d5bbe bfda9be c95e466 a977f8c 33ded96 + prior 19 (d2e2b3b..a02eb0a, count at this commit). Tree clean.

VERDICT: PASS — Day 11 dashboard + compliance plane green offline (deploy/config guarded, offline proof is the gate). Day 12 unblocked.
