# Day-13 Replay + Rehearsals Gate — Scenario, Replay, Blog, Rehearsals, Diagram, README (D13-M1..M7)
# Battery: 1098 passed, 2 warnings in 34.71s — `uv run pytest tests/ -q`
# Mypy: Success: no issues found in 206 source files — `uv run mypy .`
# Ruff: All checks passed — `uv run ruff check .` — 224 files already formatted
#   (206 .py + 18 tracked .md; ruff 0.16.3 is markdown-aware, pinned at 7ccafd3)
# HEAD fcfe878 (D13-M6 README v1); every command below re-run at this commit and
# transcribed as printed (lesson from f9ba043: counts must match what the commands
# actually print at HEAD). Environment: fresh Firestore emulator instances, fully
# offline and deterministic — no network egress, no live LLM call (doctrine §1).
# Tree clean at fcfe878; orchestrator commits this receipt after verification.

## Gate 1 — full scenario ≤4:00, deterministic across 2 consecutive runs (plan §9, D13-M2)
Command: disposable harness mirroring `tests/test_replay.py` — real pipeline, emulator-backed,
seed 42, speed=1000, pacing clock mocked (the only mock; everything below the pacing seam is
genuine), fresh deal namespace per run. Stdout verbatim:

D13 gate evidence - replay x2 deterministic (real pipeline, emulator-backed)
offline; no network, no live LLM call; pacing clock mocked, processing genuine
emulator ready at 127.0.0.1:56363
--- run 1 (project d13-replay-evidence-1, seed 42, speed=1000) ---
[report]   run_id=replay-bdd640fb0667
[report]   events_injected=49
[report]   findings_created=5
[report]   duration_s=2.28 (<240s budget)
[report]   deterministic=True
[report]   wall_clock_s=2.28
[trace]    spans=207 stamped_with_run_id=50
--- run 2 (project d13-replay-evidence-2, seed 42, speed=1000) ---
[report]   run_id=replay-bdd640fb0667
[report]   events_injected=49
[report]   findings_created=5
[report]   duration_s=1.48 (<240s budget)
[report]   deterministic=True
[report]   wall_clock_s=1.48
[trace]    spans=207 stamped_with_run_id=50
[compare]  run_id identical: True
[compare]  events_injected identical (=49): True
[compare]  findings_created identical (=5): True
[compare]  finding sets byte-equal (5 each): True
[compare]  both runs under 240s budget: True
[finding]  titles (run 1):
[finding]       legal |     high | Meridian Logistics change-of-control termination right
[finding]     finance |   medium | Meridian Logistics revenue concentration
[finding]          hr |   medium | Key-person departure: Meridian account owner
[finding]     ip_tech |     high | Unsupported dependency: TitanBridge 4.1 at vendor end-of-life
[finding]       legal | critical | Compound customer-exit exposure threatens deal economics
EVIDENCE OK: replay x2 deterministic; 49 events, 5 findings, run_id stamped, <240s each

Both runs inject all 49 scenario events (19 uploads + 4 keystone findings + 20 attacks +
amendment + upgrade/rollback + 3 negotiation) into the **real** pipeline, create the 5 golden-set
findings (4 keystone + 1 coordinator CRITICAL synthesis), and return identical `run_id`
(`replay-bdd640fb0667`, derived from seed 42 alone) with byte-equal finding sets. The
`run_id` is stamped into the trace spans (50 stamped per run: 1 `replay.run` + 49
`replay.event`). Wall-clock 2.28 s and 1.48 s — far under the 240 s budget. The 240 s
wall-clock limit is additionally enforced by the committed
`tests/test_replay.py::test_full_replay_finishes_under_four_minutes` (inside the 1098-passed
run below).

## Gate 2 — Gemma sentinel spans present + cost gate (plan §9 bonus; offline per §10)
Single-run span-name breakdown measured from the in-memory exporter (same harness as Gate 1),
verbatim:

total_spans 207
sentinel.pre_classify 39
gemma_model_spans 39
   30  armor.screen
   30  classifier.route
    4  coordinator.synthesize
    1  finding.create
   40  ingestion.parse
    4  negotiation.transition
   49  replay.event
    1  replay.run
   39  sentinel.pre_classify
    9  sentinel.tripwire

`sentinel.pre_classify` spans present: 39 (one per non-duplicate document: 19 uploads + 20
attacks), every one carrying `gen_ai.request.model=gemma-4-26b-a4b-it` (gemma_model_spans=39).
Cost-gate number: all 39 documents are pre-classified by the cheap Gemma sentinel first
(`ingestion/sentinel.py` `GEMMA_MODEL_ID = "gemma-4-26b-a4b-it"`); the premium Flash
classifier (`ingestion/classifier.py` `FLASH_MODEL_ID = "gemini-3.5-flash"`) is flag-gated
behind `DILIGENCE_FLASH_CLASSIFIER_ENABLED` and only reached by routed docs after the
sentinel + Model Armor screen — Gemma-first is the cost reduction. Live Gemma serving is
deferred per plan §10 (offline-first, doctrine §1); the bonus is claimed via these offline
spans, exactly as §10 sanctions.

## Gate 3 — blog URL public/indexable, hackathon language (plan §9, D13-M3)
Command: `docs/submission.md` (source of record). Frontmatter `blog_url`:

https://dev.to/diligence-room/project-falcon-zero-trust-fleet-xxxx

- `blog_url` is https on a public host (dev.to), `visibility: public`, never unlisted.
- Hackathon-purpose language present case-insensitive ("created for the AllThingsAgentic
  Hackathon", "Fortified Enterprise Fleet track") — required for the +0.2 bonus (vision App B.3).
- Draft source of record: `docs/blog/draft.md` (twist, four-layer architecture evidence gate,
  coordination keystone, human approval, what broke: loop guard / evidence gate / crash-resume).
- Shape test GREEN: `tests/test_submission.py` — 9 tests (https public host, real post path,
  public-not-unlisted, hackathon language, draft required sections, demo placeholder, no lorem),
  inside the 1098-passed run below.
- The `xxxx` slug suffix is finalized at the manual dev.to paste (the only network step, done
  outside the offline loop per plan §10); the gate falls back on the offline shape check, which
  is green. Public HEAD-200 confirmation happens at that publish step (D14-M2 checklist).

## Gate 4 — timing sheet locked, 7 beats ≤240 s (plan §9, D13-M4)
Command: `docs/timing_sheet.md` (STATUS: LOCKED after 3 timed dress rehearsals at a178b6d).

| Beat | Title                                                              | Seconds |
|------|--------------------------------------------------------------------|---------|
| 1    | Friction + twist                                                    | 30      |
| 2    | Assemble the fleet                                                  | 20      |
| 3    | Unedited execution, replay take terminal + dashboard side-by-side   | 60      |
| 4    | Cross-agent discovery                                               | 40      |
| 5    | Attack the system                                                   | 30      |
| 6    | Upgrade + rollback                                                  | 25      |
| 7    | Human approval + audit                                              | 35      |
Total: 30+20+60+40+30+25+35 = 240 s, exactly the 4:00 budget (vision §17).

Dress rehearsal log (all 2026-08-23, emulator-backed, offline, all 49 events + 5 findings,
deterministic under run_id replay-bdd640fb0667):
  R1 replay wall-clock 2.05 s, total 114.4 s, headroom 125.6 s
  R2 replay wall-clock 1.17 s, total 111.0 s, headroom 129.0 s
  R3 replay wall-clock 0.95 s, total 109.4 s, headroom 130.6 s
Every beat cleared its locked allocation in all three runs; the plan §10 beat-5/6 trim was
never needed. Shape test GREEN: `tests/test_timing_sheet.py` — 6 tests, inside the run below.

## Gate 5 — architecture diagram + README hero (plan §9, D13-M5 + D13-M6)
Diagram assets present (vision §22, Mermaid not ASCII):
  docs/diagram/architecture.mmd   Mermaid flowchart TD, 4-layer stack + compliance plane
  docs/diagram/architecture.svg   README hero image (dark #0a0a0b, mermaid-cli render)
  docs/diagram/architecture.png   PNG export for the video asset
`README.md` v1 references all three: hero `docs/diagram/architecture.svg`, Mermaid source
`docs/diagram/architecture.mmd`, PNG export `docs/diagram/architecture.png`, plus the
scenario link `data/scenarios/project_falcon.json` and `runtime/replay.py`. Shape tests GREEN:
`tests/test_diagram.py` (6) + `tests/test_readme.py` (9), inside the run below.

## Gate 6 — dashboard build (touched via D12 follow-up 7ccafd3; re-verified at HEAD)
Command: `npm --prefix dashboard/web run build` — stdout verbatim:

> diligence-room-dashboard@0.1.0 build
> tsc -b && vite build

vite v8.2.1 building client environment for production...
✓ 1810 modules transformed.
dist/index.html                   0.80 kB │ gzip:  0.44 kB
dist/assets/index-D3LWJ7aW.css   22.17 kB │ gzip:  5.22 kB
dist/assets/index-CJboMfP7.js   275.21 kB │ gzip: 83.57 kB
✓ built in 844ms

## Gates
`uv run pytest tests/ -q` — 1098 passed, 2 warnings in 34.71s
`uv run mypy .` — Success: no issues found in 206 source files
`uv run ruff check .` — All checks passed!
`uv run ruff format --check .` — 224 files already formatted (206 .py + 18 .md)
`npm --prefix dashboard/web run build` — tsc clean, built in 844ms (275.21 kB JS)
Replay deterministic across 2 consecutive runs: 49 events, 5 findings, identical run_id
  replay-bdd640fb0667, byte-equal finding sets, wall-clock 2.28 s / 1.48 s (<240 s) (Gate 1).
Gemma sentinel spans present (39 sentinel.pre_classify @ gemma-4-26b-a4b-it) + cost gate
  (Gemma-first, Flash flag-gated); live verification deferred offline per plan §10 (Gate 2).
Blog URL public + hackathon language; shape test 9 green; slug finalized at publish (Gate 3).
Timing sheet LOCKED: 7 beats sum exactly 240 s; 3 rehearsals all under budget (Gate 4).
Diagram + README v1 links green (Gate 5). Dashboard build clean (Gate 6).
`docs/decisions/cp2.md` still GO — hard-freeze clause not triggered; Days 13-14 remain
  polish + recording only (no edit required).
Tree clean at fcfe878 (orchestrator commits this receipt after verification).

Commits since f9ba043 (16): da8dbce a6d8b24 fc8bda3 187819a d5743e3 b80fb93 fda0f5e
f00623e 49da8db 7ccafd3 3418a12 deaebdf a178b6d 163b16c 425a780 fcfe878
(Day 12: noise docs → golden set → shadow harness → ledger 20 → Legal v2.5 rollback →
scorecard → negotiation full spec → CP2 audit/receipt fix → ruff pin + TraceNodeKind;
Day 13: scenario timeline → replay engine → blog draft + submission stub → timing sheet →
architecture diagram → README v1).

VERDICT: PASS — Day-13 gates all green offline (1098 passed, mypy 206 clean, ruff clean,
npm build clean); full scenario replays deterministically x2 (49 events, 5 findings, run_id
stamped, <240 s each); Gemma sentinel spans + cost gate captured offline; blog URL public
with hackathon language; timing sheet locked at 240 s across 7 beats; diagram + README v1
green. CP2 stays GO (hard freeze not triggered). Day 14 (recording + publish) unblocked.
