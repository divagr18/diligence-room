# D2 Live Gate Evidence — Day-2 gate chain on LIVE GCP (captured 2026-08-16)

Project: diligence-room (910285417505), region us-central1 / Firestore nam5.
Project was undeleted for this gate, run end-to-end live, and torn down again
afterwards (see d2-teardown.txt).

## 1. Data room provisioned (infra/data_room.py --confirm-live)

```
created topic deal-events
created bucket gs://diligence-room-dataroom-deal-falcon-us (us-central1)
created bucket gs://diligence-room-dataroom-deal-falcon-eu (europe-west1)
IAM grant serviceAccount:service-910285417505@gs-project-accounts.iam.gserviceaccount.com -> roles/pubsub.publisher (on deal-events)
created notification on gs://diligence-room-dataroom-deal-falcon-us -> deal-events
created notification on gs://diligence-room-dataroom-deal-falcon-eu -> deal-events
created subscription deal-events-sub -> deal-events
Data room ready for deal-falcon.
```

Verified via gcloud: `pubsub topics describe deal-events`,
`subscriptions describe deal-events-sub` (topic wired),
`storage buckets list` (US-CENTRAL1 + EUROPE-WEST1 pair),
`pubsub topics get-iam-policy deal-events` (GCS P4SA = pubsub.publisher),
`storage buckets notifications list` (event_types=[OBJECT_FINALIZE], payload JSON_API_V1).

Note: the GCS project service agent (gs-project-accounts) needed propagation
time after the project undelete before the IAM binding was accepted.

## 2. Deal workspace provisioned (live Firestore)

```
deal-falcon provisioned in LIVE Firestore
exists: True | fields: created_at, deal_id, deal_type, expected_window_days,
name, policy_profile_id, regions, status, target
```

## 3. Hello agent deployed + async invoke (SA6, also Day-1 gates M6/M7)

```
Deployed: projects/910285417505/locations/us-central1/reasoningEngines/5096132490892935168
invoke -> Session: 4459257903970254848
Agent response: 'The echoed text is: diligence-room-day1-smoke'
PASS: deployed agent answered async with the echo marker
```

Model facts verified live: gemini-3.5-flash is served ONLY from the `global`
location (regional endpoints return 404; probe tested us-central1/us-east1/
us-east5/europe-west1/us-west1). Agent Engine runs in us-central1 and receives
GOOGLE_CLOUD_LOCATION=global via env_vars (GOOGLE_CLOUD_PROJECT is reserved).
Local smoke: `[smoke] PASS` (echo tool called + marker in response).

## 4. Upload -> notification -> consumer -> audit + deal-state + agent (SA7)

```
$ gcloud storage cp data/acme_robotics/contract_customer_x.pdf \
    gs://diligence-room-dataroom-deal-falcon-us/
```

Live consumer (runtime/consumer.py --confirm-live --watch):

```
Session: 6669399421102325760          <- hello agent invoked on Agent Engine
processed=1 duplicates=1 skipped=0    <- one notification processed, one redelivery deduped
```

Firestore state after processing (live read):

```
documents_ingested: 1
last_document_id: contract_customer_x.pdf
last_ingested_at: 2026-08-15 23:07:15.267532+00:00
event count: 1
seq 1 | document.ingested |
  {"bucket":"diligence-room-dataroom-deal-falcon-us","content_type":"application/pdf",
   "document_id":"contract_customer_x.pdf"} | dedupe: 32b84db61414f947...
```

Integration fix found by the live run (not catchable offline): GCS bucket
notifications carry eventType in message ATTRIBUTES while the storage-object
JSON in message.data carries name/bucket/contentType. PubSubPullSource now
normalizes both into the flat payload contract (`_normalize_gcs_notification`).

## 5. Duplicate idempotency (re-upload same object)

```
$ gcloud storage cp data/acme_robotics/contract_customer_x.pdf \
    gs://diligence-room-dataroom-deal-falcon-us/   # overwrite -> new notification
consumer -> processed=0 duplicates=1 skipped=0
documents_ingested (expect still 1): 1
event count (expect still 1): 1
```

The re-upload produced a notification with the same dedupe key; the consumer
recognized the existing audit record and skipped re-processing. Deal state and
event log are unchanged.

## 6. Registry seed + live API demo (SA4)

Provenance: executed live during the gate session; outputs recovered from the
session transcript into this file during verification (persistence was missed
at capture time; values match the session record exactly; re-run impossible
while the project stays DELETE_REQUESTED by instruction).

```
$ uv run python registry/seed.py --confirm-live
Seeded 8 new manifest(s); registry now holds 8 agent(s).

$ uv run uvicorn registry.server:app --host 127.0.0.1 --port 8451   # live Firestore via ADC
INFO:     Application startup complete.
INFO:     Uvicorn running on http://127.0.0.1:8451 (Press CTRL+C to quit)

$ curl -s http://127.0.0.1:8451/agents
-> 8 manifests, all approved=true, policy_profile=falcon-standard-v1,
   required_identity={ws}-agent@deal, allowed_tools=[data-room-read, finding-create, gateway-query]:
   esg=1.3.0  finance=3.1.0  hr=1.8.0  ip_tech=2.2.0
   legal=2.4.0  real_estate=1.1.0  regulatory=2.0.0  tax=1.5.0

$ curl -s http://127.0.0.1:8451/agents/legal/versions
[{"version":"2.4.0","model_id":"gemini-3.5-flash",
  "prompt_ref":"agents.legal.prompts:SYSTEM_PROMPT",
  "created_at":"2026-08-15T21:51:22.386335Z","approved":true,
  "eval_score":null,"rollback_target":null,"changelog":""}]

$ curl -s -X PATCH http://127.0.0.1:8451/agents/legal/approval \
      -H "Content-Type: application/json" \
      -d '{"approved": true, "eval_score": 0.97, "rollback_target": "2.3.0"}'
{"agent_id":"legal","name":"Legal Agent","version":"2.4.0",...,
 "approved":true,"eval_score":0.97,"deployment_status":"registered",
 "rollback_target":"2.3.0",...}

$ verification readback
legal: approved=True eval_score=0.97 rollback_target=2.3.0
```

## 7. Gateway on Cloud Run (SA5)

Same provenance note as section 6.

```
$ uv run python infra/deploy/cloud_run.py --project diligence-room \
      --service gateway --region us-central1 --source . --allow-unauthenticated --confirm-live
Deploy submitted: gcloud run deploy gateway --source . --region us-central1 \
  --project diligence-room --set-build-env-vars=GOOGLE_PYTHON_VERSION=3.13 --allow-unauthenticated

URL: https://gateway-7ur7rxmhma-uc.a.run.app

Discovery: GET /healthz returned Google Frontend's own 404 page — GFE answers
/healthz itself at the edge before the container sees the request. /health
alias added (commit ee03bd8); app serves both routes locally.

$ curl -s https://gateway-7ur7rxmhma-uc.a.run.app/health
{"status":"ok","service":"gateway"}  [HTTP=200]

$ curl -s -H "X-Caller-Identity: day2-live-audit" https://gateway-7ur7rxmhma-uc.a.run.app/whoami
{"caller":"day2-live-audit"}  [HTTP=200]

$ curl -s https://gateway-7ur7rxmhma-uc.a.run.app/whoami
{"caller":"anonymous"}
```
