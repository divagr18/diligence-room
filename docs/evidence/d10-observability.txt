# Day-10 Offline Gate — Observability (D10-M1..M5 + trace linking + graph)
# Battery: 911 passed, 2 warnings in 22.05s — `uv run pytest tests/ -q`
# Mypy: Success: no issues found in 175 source files — `uv run mypy .`
# Ruff: All checks passed — `uv run ruff check .` — 188 files already formatted

## Wave 1 — D10-M1 OTel backbone (observability/otel.py)
ServiceName per fleet component + lazy Cloud Trace seam via BatchSpanProcessor.
`uv run pytest tests/test_otel.py -q` — 6 passed in 0.10s
- offline provider carriers service.name; providers isolated per call
- live=False never imports opentelemetry.exporter.cloud_trace
- live seam batches into injected InMemorySpanExporter, trace_id_of 32-hex

## Wave 2 — D10-M2 stage instrumentation (observability/tracing.py stage_span)
`uv run pytest tests/test_stage_spans.py -v` — 9 passed in 5.00s
- stage_span(None) yields None, tracer path opens span with attributes
- armor.screen blocked+rule_count from pipeline tracer branch
- agent.tool (data_room_read/finding_create) principal/workstream/decision
- gateway.decide verdict/reason
- coordinator.synthesize accepted/contributors/entity (refusal included)
- negotiation.transition chain 4 spans -> draft/pending_approval/approved/send_logged
All queued behind the existing ingestion spans (parse/sentinel/tripwire/classifier.route) — no drift.

## Wave 3 — D10-M4 lineage carrier + audit_trace_id + trace_link.resolve
Lineage documents carry trace_id/span_id via record_span_context (merge=True sets preserve).
Finding writers open a finding.create span linked to every cited document's ingestion span.

`uv run pytest tests/test_trace_link.py -v` — 7 passed
- ingest records parse span context (trace_id 032x, span_id 016x) == footer equality
- tracerless ingest leaves no span context
- finding_create sets audit_trace_id (32-hex) + 1 finding.create link == ingest trace
- tracerless finding keeps audit_trace_id None
- resolve links finding -> ingestion sources (trace_id equality)
- unknown finding -> None; synthesis with tracer -> audit_trace + sources (fleet-seeded without tracer => trace None)

`uv run pytest tests/test_trace_link.py tests/test_coordinator.py tests/test_tool_finding_create.py -q` — 50 passed in 7.69s

## Wave 4 — D10-M3 finding-graph trace serializer (dashboard/api)
FindingGraph (documents->agents->gateway->finding->escalation) for SYN-001;
_default_graph doc->agent->finding chains for other findings. Frontend typed timeline with kind glyphs.

`uv run pytest tests/test_dashboard_api.py -q` — 24 passed
- synthesis graph carries the full chain (kinds document/agent/gateway/finding/escalation, node connectivity)
- regular findings get a source-chain graph
`npm run build` — dist 0.80kB/21.84kB/270.13kB, built in 3.27s
`node qa-contract.mjs` — ALL DESIGN-CONTRACT CHECKS PASS (vite --host 127.0.0.1 fix, 127.0.0.1 probes; listeners 0 after teardown)

## Wave 5 — D10-M5 attack batch #2 (ledger 14) + noise docs
New sentinel pattern encoded_injection (data:text/html;base64 | eval(atob()) for A-2 smuggling).
4 fixtures: injection/encoded_a,b sentinel_tripwire encoded_injection; exfiltration/c armor exfiltration; cross_ws/privilege_escalation_b armor privilege_escalation.
3 noise docs in data/vantage_robotics (email_thread_export, scanned_memo_vendor, quarterly_budget_notes) — all clean.

`uv run pytest tests/test_redteam_runner.py tests/test_redteam_fixtures.py -q` — 18 passed in 6.81s
`uv run pytest tests/test_redteam_runner.py tests/test_armor_rules.py tests/test_sentinel.py -q` — 57 passed in 7.28s
`uv run pytest tests/ -q` — 911 passed, 2 warnings in 22.05s (ledger 14/14)
Runner scorecard: injection 6/6, exfiltration 3/3, cross_ws 3/3, poisoning_cross_deal 2/2, TOTAL 14/14 blocked
Dataset zero-false-positive on every committed document (including 3 noise docs) still green.

## Wave 6 — live window (scripts/run_d10_live_trace.py) — ONE approved execution
Guarded: refuses without --confirm-live and under FIRESTORE_EMULATOR_HOST; env contract GOOGLE_CLOUD_PROJECT + DILIGENCE_TRACE_ENABLED (no import-time defaults).
Built via observability.otel build_provider(live=True) -> CloudTraceSpanExporter + BatchSpanProcessor; same tracer seam tests cover with in-memory exporter.

Live attempt (2026-08-22, --confirm-live, deal-trace-live, ~50 spans, free tier):
$env:GOOGLE_CLOUD_PROJECT="diligence-room"; $env:DILIGENCE_TRACE_ENABLED="1"; uv run python scripts/run_d10_live_trace.py --confirm-live
-> StatusCode.PERMISSION_DENIED on Firestore (projects/diligence-room, service firestore.googleapis.com, message "Permission denied on resource project diligence-room", consumer projects/diligence-room). gcloud credentialed as keshav.agr2007@gmail.com, project diligence-room, ADC token present, but Firestore write not authorized in this IAM context.
Result per plan: built-guarded + offline proof is the gate. No retries (spend discipline). The chain is proven offline via the same tracer seam + trace_link.resolve (above); the Cloud Trace print path is intact (provider.force_flush + Console URL) once IAM is granted.

## Gates
`uv run pytest tests/ -q` — 911 passed
`uv run mypy .` — Success: no issues found in 175 source files
`uv run ruff check .` — All checks passed
`uv run ruff format --check .` — 188 files already formatted
`npm run build` — dist generated, tsc clean
`node qa-contract.mjs` — ALL PASS
redteam 14/14 blocked + dataset 0 false positives + finding-graph served

Commits since d2e2b3b (18): a5b53bc b2c293e f4412a8 fd0d864 5d11812 + prior 13 (a02eb0a..6ee960d). Tree clean.

VERDICT: PASS — Day 10 observability green offline (live window built-guarded; one live attempt recorded, permission denied, no retry). Day 11 unblocked.
