DAY 8 OFFLINE GATE (BUILD_PLAN Day 8: coordinator + red-flag engine)
Executed (UTC): 2026-08-20
Scope: red-flag scoring (D8-M1), human->agent-output AuthZ (D8-M2),
cross-workstream keystone synthesis (D8-M3), role-filtered dashboard APIs
(D8-M4), all offline against the Firestore emulator. The keystone-set item
"Customer X cross-workstream finding" is now produced by genuine multi-agent
synthesis, not authored demo data.

====================================================================
1. QUALITY BATTERY (full suite, emulator-backed)
====================================================================
$ uv run pytest tests/ -q
854 passed, 2 warnings in 20.41s

$ uv run mypy .
Success: no issues found in 163 source files

$ uv run ruff check .
All checks passed!
$ uv run ruff format --check .
175 files already formatted

====================================================================
2. GATE ITEM A - red-flag scoring reproduces vision S10 (D8-M1)
====================================================================
$ uv run pytest tests/test_scoring.py -q
19 passed in 0.09s

Canonical example (vision S10): Legal HIGH + 18.3% revenue exposure +
0.94 confidence + Legal & Finance affected => CRITICAL. Legal alone
(single workstream) stays HIGH — the multi-workstream blast radius is what
tips it. Severity-loyal without escalation context; ceiling is one step
above the finding's severity when escalated. Deterministic + monotonic
(higher confidence / exposure / blast radius / unresolved duration never
lowers the score); rationale names every factor.

====================================================================
3. GATE ITEM B - keystone emerges ONLY from multi-agent convergence (D8-M3)
====================================================================
$ uv run pytest tests/test_coordinator.py -v
tests/test_coordinator.py::TestKeystoneSynthesis::test_all_four_workstreams_yield_a_critical_finding PASSED
tests/test_coordinator.py::TestKeystoneSynthesis::test_synthesis_escalates_and_logs_the_finding PASSED
tests/test_coordinator.py::TestKeystoneSynthesis::test_synthesis_scores_critical_under_red_flag_engine PASSED
tests/test_coordinator.py::TestRemovalProof::test_removing_any_single_workstream_refuses_synthesis[legal] PASSED
tests/test_coordinator.py::TestRemovalProof::test_removing_any_single_workstream_refuses_synthesis[finance] PASSED
tests/test_coordinator.py::TestRemovalProof::test_removing_any_single_workstream_refuses_synthesis[hr] PASSED
tests/test_coordinator.py::TestRemovalProof::test_removing_any_single_workstream_refuses_synthesis[ip_tech] PASSED
tests/test_coordinator.py::TestRemovalProof::test_no_findings_at_all_refuses_synthesis PASSED
tests/test_coordinator.py::TestIntegrityAndIdempotency::test_tampered_evidence_span_refuses_synthesis PASSED
tests/test_coordinator.py::TestIntegrityAndIdempotency::test_rerun_is_idempotent_and_does_not_duplicate PASSED
10 passed in 5.46s

- Four deep workstreams (legal/finance/hr/ip_tech) seeded through the SAME
  evidence-gated finding_create path -> coordinator writes ONE CRITICAL
  finding: inherited evidence (4 spans) re-verified against source docs,
  related_findings links all four contributors, deal-lead inbox entry +
  finding.escalated event, stable content-derived id (idempotent rerun).
- REMOVAL-PROOF (BUILD_PLAN Day-8 verification): dropping ANY single
  contributor workstream refuses synthesis — removing one workstream input
  prevents the CRITICAL finding, proof it is not a single-agent result.
- Tampered evidence span refused; synthesis scores CRITICAL under the D8-M1
  red-flag engine (18.3% exposure, 4 workstreams).

====================================================================
4. GATE ITEM C - human->agent-output AuthZ on the dashboard (D8-M2 + D8-M4)
====================================================================
$ uv run pytest tests/test_human_authz.py tests/test_dashboard_api.py -q
102 passed, 1 warning in 0.89s

Role matrix (vision S7.4): deal_lead sees everything; junior_legal sees
legal findings only (no valuation/finance output); outside_counsel sees
legal materials approved for external eyes (validated/resolved) only;
hr_analyst sees HR only. Wired as an optional ?role= query param on
/api/findings, /api/findings/{id}, and the /api/deal inbox; default remains
deal_lead. Zero-trust: a role-hidden finding returns 404 — indistinguishable
from a missing one; unknown role -> 422.

$ curl surface evidence (uvicorn dashboard.api.app:app, 127.0.0.1:8040):
GET /api/findings                           -> 200, findings=11
GET /api/findings?role=deal_lead            -> 200, findings=11
GET /api/findings?role=junior_legal         -> 200, findings=3 (SYN-001, LEGAL-014, LEGAL-021)
GET /api/findings?role=outside_counsel      -> 200, findings=2 (LEGAL-014, LEGAL-021)
GET /api/findings?role=hr_analyst           -> 200, findings=1 (HR-003)
GET /api/findings/FIN-007?role=junior_legal -> 404
GET /api/findings/SYN-001?role=outside_counsel -> 404
GET /api/findings?role=intern               -> 422

====================================================================
5. HONEST STAND-INS (carried forward, disclosed)
====================================================================
- Live windows NOT executed in the offline loop (zero live spend):
  scripts/run_d8_live_coordinator.py is built + guarded (--confirm-live,
  refuses the emulator, env contract); guards verified (--help exits 0,
  exit 1 without --confirm-live). Same posture as the d6/d7 live windows.
- The coordinator is a deterministic aggregation principal that keeps the
  evidence gate intact: it inherits contributor-verified evidence and
  re-verifies every span at synthesis time (vision S19.3 defense in depth);
  it never fabricates a span and does not write through finding_create (its
  single-workstream AuthZ would deny cross-workstream evidence).
- Dashboard API still serves the authored demo data plane (read-only); the
  Firestore-backed repository swap is Day 11. The role filter is enforced
  server-side via the identity.human_authz Viewable protocol for both.
- FakeSentinel remains the offline sentinel (docs/decisions/gemma-serving.md).

====================================================================
6. DAY 8 COMMITS (this gate)
====================================================================
b9d7a4f feat(coordination): red-flag scoring engine (D8-M1)
87f0b8d feat(identity): human-output-authz role matrix (D8-M2)
1fc5fe1 feat(dashboard): role-filtered findings API (D8-M4)
09036d6 feat(coordinator): cross-workstream synthesis keystone (D8-M3)
c3a0424 feat(coordinator): Day-8 live window script, guarded, not executed

VERDICT: PASS - Day-8 keystone proven offline with real-surface artifacts:
the CRITICAL finding emerges only from 4-way convergence (removal-proof),
red-flag scoring reproduces vision S10, and human-output AuthZ filters every
dashboard surface. Day 9 (failure tolerance, CUTLINE-1) unblocked.
