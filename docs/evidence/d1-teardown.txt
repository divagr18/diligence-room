# Day 1 Teardown Record - 2026-08-15 (user-requested, "for now")

## Removed

| Resource | Identifier | Command | Result |
|---|---|---|---|
| Billing budget | billingAccounts/012D4F-261885-E006CE/budgets/31bb8b60-1fad-439f-a723-c1989640a3d6 | gcloud billing budgets delete | "Deleted budget [31bb8b60-...]" exit 0 |
| Staging bucket | gs://diligence-room-staging (us-central1) | gcloud storage buckets delete | removed (buckets list empty) |
| GCP project | diligence-room (number 910285417505) | gcloud projects delete --quiet | lifecycleState=DELETE_REQUESTED |

## Restored

- `gcloud config set project pull-guard-prod` (original default restored; verified).

## Notes

- Project deletion is scheduled; recoverable ~30 days via `gcloud projects undelete diligence-room`.
- No Agent Engine deployment ever existed (runtime was ADC-blocked).
- Zero service-account keys were created at any point (guardrail verified before teardown).
- The billing account itself (012D4F-261885-E006CE) was not modified beyond the budget delete.
- Full recreation is one command pair: `uv run python infra/bootstrap_gcp.py` then
  `uv run python infra/guardrails.py` (idempotent, committed in this repo).
- GitHub repository divagr18/diligence-room is intentionally NOT part of this teardown
  (user scope: Google Cloud only).
