# Demo narration - the interactive cut

Generated by `scripts/video/interactive_script.py`. Timed captions live
in `demo_script.srt`; this file is the read-aloud version.

Every caption has an action under it. Segment windows are fixed; cue
times inside a window are split by word count.

## 1. The find (0:00-0:26)

*findings table, scrolls the list, filters critical -> high -> all, opens the critical*

- **   0.5s** Eight AI agents just finished reading this company's entire data room.
- **   6.2s** Five findings. Filtering to the one that matters: a single critical.
- **  11.8s** Two high as well. Every row is a finding an agent wrote, with its evidence attached.
- **  19.9s** This critical one is the one that changes the price. Opening it.

## 2. Evidence (0:26-0:58)

*scrolls summary -> evidence spans, clicks "Open source", the viewer opens*

- **  26.5s** The target's largest customer can walk the moment the deal closes, taking eighteen percent of next year's revenue.
- **  34.6s** Ninety percent confidence, four source documents, four contributing agents. No single agent could call this alone.
- **  41.8s** I'm Divyansh Agrawal, and this is Diligence Room, my entry for the Fortified Enterprise Fleet track.
- **  49.0s** Every finding quotes the clause it came from, and I can open the source document right here, at that line.

## 3. Trace (0:58-1:28)

*scrolls scope -> open questions -> finding graph -> audit trace*

- **  58.5s** Scope shows which agents contributed, and what each one filed on its own.
- **  66.2s** The finding graph traces it back: which documents, which agents, which gateway decision.
- **  73.8s** And the audit trail. Document parsed, finding created, gateway decision, escalation.
- **  80.3s** Any claim the fleet makes can be walked back to the source text.

## 4. The room (1:28-1:56)

*deal room scrolled to the escalation inbox, then the Documents tab*

- **  88.5s** That was one finding. Here is the whole deal room: coverage per workstream, documents ingested, threats blocked.
- **  97.5s** And the escalation inbox, which is what the fleet decided a human needs to see.
- ** 105.5s** Every document the agents read is listed, with the workstream it was routed to and the confidence behind that call.

## 5. Security (1:56-2:26)

*scrolls the full quarantine table*

- ** 116.5s** Analysts are not the only thing reading these files. I hit this fleet with twenty red-team attacks.
- ** 127.4s** Prompt injection, encoded payloads, data theft, cross-agent writes. All twenty blocked, zero false positives.
- ** 136.4s** Gemma 4 screens every file, then Model Armor. Nothing here ever reached an agent runtime.

## 6. Platform (2:26-2:52)

*Agent Registry listing + Memory Bank recall*

- ** 146.5s** The whole fleet is published into the Agent Registry on Gemini Enterprise Agent Platform, discoverable company-wide.
- ** 156.2s** All eight, with versions, approval state and eval scores.
- ** 161.8s** Memory Bank holds what we know about this buyer across sessions, retrieved here in a fresh process.

## 7. Human-in-the-loop (2:52-3:30)

*clicks Draft -> expands the draft body -> Approve -> Record send*

- ** 172.5s** Now the part that decides whether you can run this on a real deal. The negotiation agent can draft, but it cannot send.
- ** 186.0s** I click draft. It writes the redline, quoting the evidence it just showed me.
- ** 194.4s** It stops at pending approval. Nothing leaves the building until a human says so.
- ** 202.8s** I approve it. Now, and only now, can the send be recorded.

## 8. Google Cloud proof (3:30-3:58)

*Cloud Logging + Cloud Trace*

- ** 210.5s** That approval lands in Cloud Logging as a request against Cloud Run.
- ** 217.5s** Agent Runtime, Cloud Run in two regions, Firestore and Pub/Sub, all live on Google Cloud.
- ** 226.1s** And the same call in Cloud Trace, span by span, so an auditor can follow any claim back to the source.
